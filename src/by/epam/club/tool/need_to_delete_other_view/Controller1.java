/*
package by.epam.club.tool.need_to_delete_other_view;

import by.epam.club.exception.ControllerException;
import by.epam.club.tool.need_to_delete_other_view.for_using.controller.AbstractSharingPicController;

import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet(name = "/controller1")
public class Controller1 extends AbstractSharingPicController {
    protected void doGet(HttpServletRequest request, HttpServletResponse response) {
        try {
            processRequest(request, response, new RequestContent());
        } catch (ControllerException e) {
            e.printStackTrace();
        }
    }

    protected void doPost(HttpServletRequest request, HttpServletResponse response) {
        try {
            processRequest(request, response, new RequestContent());
        } catch (ControllerException e) {
            e.printStackTrace();
        }
    }
}
*/
